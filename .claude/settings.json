{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "statusMessage": "Regenerando arquivos .g.dart e .freezed.dart...",
            "command": "$d = ($input | Out-String | ConvertFrom-Json).tool_input.file_path; if ($d -and $d -match '\\.dart$' -and $d -notmatch '\\.(g|freezed)\\.dart$') { dart run build_runner build }",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
